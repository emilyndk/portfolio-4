---
const { variant= "primary", size = "desktop", input } = Astro.props;

// gælder for alle //
const buttonStyling = " transition-all duration-300 ease-in-out hover:bg-obsidian border rounded-full font-p-light  ";


const variantClasses ={
"primary": "border-obsidian text-obisidan hover:bg-obsidian hover:text-mist hover:border-mist",
"secondary": "",

};

const sizeClasses ={
    "desktop": "text-button-d px-padding-m py-padding-xs",
    "mobil": "text-button-m px-padding-xs py-padding-xxs"
};



---

<button class="buttonComponent" class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot />
   <div class=""> {input} </div>
</button>